workflows:
  ios-workflow:
    name: Pax Rio Verde iOS Final Build
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      groups:
        - "Pax Rio Verde"
      vars:
        XCODE_PROJECT: "iosApp/iosApp.xcodeproj"
        XCODE_SCHEME: "iosApp"
        APP_ID: "br.com.paxrioverde.app"
        TEAM_ID: "264TWUW2L6"
        PROV_NAME: "Pax_Rio_Verde_Final"
        PROV_UUID: "f83a914d-202a-436e-9e51-257ae86fccd0"
    scripts:
      - name: Initialize Keychain
        script: keychain initialize

      - name: Install Credentials
        script: |
          # 1. Instala o Certificado P12
          echo "$CERTIFICATE_P12" | base64 --decode > /tmp/certificate.p12
          keychain add-certificates --certificate /tmp/certificate.p12 --certificate-password "$CERTIFICATE_PASSWORD"
          
          # 2. Instala o Perfil de Provisionamento
          PROFILES_DIR="$HOME/Library/MobileDevice/Provisioning Profiles"
          mkdir -p "$PROFILES_DIR"
          echo "$PROVISIONING_PROFILE" | tr -d '\r\n ' | base64 --decode > "$PROFILES_DIR/$PROV_UUID.mobileprovision"
          
          # 3. LIMPEZA TOTAL: PBXPROJ + XCCONFIG + PLIST
          # Altera em todos os arquivos do projeto para garantir que o nome antigo não persista
          find . -type f \( -name "*.pbxproj" -o -name "*.xcconfig" -o -name "*.plist" \) -exec sed -i '' "s/iOS Distribution/Apple Distribution/g" {} +
          find . -type f \( -name "*.pbxproj" -o -name "*.xcconfig" -o -name "*.plist" \) -exec sed -i '' "s/iPhone Distribution/Apple Distribution/g" {} +
          find . -name "*.pbxproj" -exec sed -i '' "s/ProvisioningStyle = Automatic;/ProvisioningStyle = Manual;/g" {} +
          
          echo "Projeto Xcode saneado totalmente para Apple Distribution."

      - name: Create Export Options
        script: |
          cat > /Users/builder/export_options.plist <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store</string>
              <key>teamID</key>
              <string>$TEAM_ID</string>
              <key>provisioningProfiles</key>
              <dict>
                  <key>$APP_ID</key>
                  <string>$PROV_NAME</string>
              </dict>
              <key>signingStyle</key>
              <string>manual</string>
              <key>manageAppVersionAndBuildNumber</key>
              <true/>
          </dict>
          </plist>
          EOF

      - name: Build and Sign IPA
        script: |
          mkdir -p build/ios/xcarchive
          mkdir -p build/ios/ipa

          # 1. LIMPEZA TOTAL DE CONFIGURAÇÕES DE ASSINATURA
          echo "🧹 Eliminando conflitos de identidade no arquivo do projeto..."

          # Deleta qualquer linha que defina CODE_SIGN_IDENTITY ou PROVISIONING_PROFILE
          # Isso remove as travas que forçam o nome "iOS Distribution"
          find . -name "project.pbxproj" -exec sed -i '' '/CODE_SIGN_IDENTITY/d' {} +
          find . -name "project.pbxproj" -exec sed -i '' '/PROVISIONING_PROFILE/d' {} +

          # Força o estilo manual em todas as configurações (Release/Debug)
          find . -name "project.pbxproj" -exec sed -i '' "s/ProvisioningStyle = [a-zA-Z]*;/ProvisioningStyle = Manual;/g" {} +

          # 2. LIMPEZA DE CACHE DO SISTEMA
          echo "🧹 Resetando caches do Xcode..."
          rm -rf ~/Library/Developer/Xcode/DerivedData/*
          rm -rf build/ios

          # 3. VERIFICAÇÃO NO LOG (Para a nossa análise)
          echo "🔍 Conferindo se sobrou algum 'iOS Distribution' escondido:"
          grep -r "iOS Distribution" . --exclude-dir=".gradle" || echo "✅ Limpo!"

          # 4. ARCHIVE COM OVERRIDE ABSOLUTO
          # Passamos a identidade duas vezes: uma geral e uma específica para o SDK, 
          # para garantir que o Xcode não use nenhum padrão antigo.
          xcodebuild clean archive \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -archivePath build/ios/xcarchive/app.xcarchive \
            -destination 'generic/platform=iOS' \
            CODE_SIGN_IDENTITY="Apple Distribution" \
            "CODE_SIGN_IDENTITY[sdk=iphoneos*]"="Apple Distribution" \
            CODE_SIGN_STYLE="Manual" \
            DEVELOPMENT_TEAM="$TEAM_ID" \
            PROVISIONING_PROFILE_SPECIFIER="$PROV_UUID" \
            PRODUCT_BUNDLE_IDENTIFIER="$APP_ID" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES \
            -allowProvisioningOffline=YES

          # 5. EXPORTAR IPA
          xcodebuild -exportArchive \
            -archivePath build/ios/xcarchive/app.xcarchive \
            -exportPath build/ios/ipa \
            -exportOptionsPlist "/Users/builder/export_options.plist"

    artifacts:
      - build/ios/ipa/*.ipa