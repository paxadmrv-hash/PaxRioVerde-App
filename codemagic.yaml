workflows:
  ios-workflow:
    name: Pax Rio Verde iOS Build
    instance_type: mac_mini_m1
    max_build_duration: 60
    environment:
      groups:
        - "Pax Rio Verde"
      vars:
        XCODE_SCHEME: "iosApp"
        APP_ID: "br.com.paxrioverde.app"
    scripts:
      - name: Set gradlew permissions
        script: chmod +x gradlew

      - name: Initialize Keychain
        script: keychain initialize

      - name: Install Manual Certificate
        script: |
          echo "$CERTIFICATE_P12" | tr -dc 'A-Za-z0-9+/=' | base64 --decode > /tmp/certificate.p12
          keychain add-certificates --certificate /tmp/certificate.p12 --certificate-password "$CERTIFICATE_PASSWORD"

      - name: Build Kotlin Shared Module
        script: ./gradlew :composeApp:assembleDebug

      - name: Build iOS App
        script: |
          # 1. Busca automática pelo workspace
          WORKSPACE_PATH=$(find . -name "*.xcworkspace" | head -n 1)
          echo "Workspace encontrado em: $WORKSPACE_PATH"

          # 2. GERA O ARQUIVO QUE ESTAVA FALTANDO (export_options.plist)
          # Isso avisa que o build é para desenvolvimento
          xcode-project use-profiles --export-type development

          # 3. Executa o build agora com as instruções de exportação criadas
          xcode-project build-ipa \
            --workspace "$WORKSPACE_PATH" \
            --scheme "$XCODE_SCHEME"

    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - paxadmrv@gmail.com